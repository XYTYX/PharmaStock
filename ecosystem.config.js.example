const path = require('path');

module.exports = {
  apps: [
    {
      name: 'pharmastock-backend',
      script: path.join(__dirname, 'backend/dist/index.js'),
      cwd: __dirname,
      env: {
        NODE_ENV: 'development',
        PORT: 3000,
        DATABASE_URL: 'file:{PROJECT_ROOT}/backend/prisma/dev.db',
        FRONTEND_URL: 'http://localhost:3001',
        JWT_SECRET: 'your-super-secret-jwt-key-change-this-in-production',
        log_file: '/dev/null',
        out_file: '/dev/null',
        error_file: '/dev/null'
      },
      env_production: {
        NODE_ENV: 'production',
        PORT: 3000,
        DATABASE_URL: 'file:/var/cache/PharmaStock/pharmastock.db',
        FRONTEND_URL: 'http://localhost:3001',
        // Production-specific log locations
        log_file: '/var/log/pharmastock/backend.log',
        out_file: '/var/log/pharmastock/backend.log',
        error_file: '/var/log/pharmastock/backend.log'
      },
      watch: false,
      instances: 1,
      exec_mode: 'fork',
      log_date_format: 'YYYY-MM-DD HH:mm:ss Z'
    },
    {
      name: 'pharmastock-frontend',
      script: 'npm',
      args: 'run dev -- --port 3001 --host 0.0.0.0',
      cwd: path.join(__dirname, 'frontend'),
      env: {
        NODE_ENV: 'development',
        log_file: '/dev/null',
        out_file: '/dev/null',
        error_file: '/dev/null'
      },
      env_production: {
        NODE_ENV: 'production',
        // Production-specific arguments
        args: 'run dev -- --port 3001 --host 0.0.0.0',
        log_file: '/var/log/pharmastock/frontend.log',
        out_file: '/var/log/pharmastock/frontend.log',
        error_file: '/var/log/pharmastock/frontend.log'
      },
      watch: false,
      instances: 1,
      exec_mode: 'fork',
      log_date_format: 'YYYY-MM-DD HH:mm:ss Z'
    }
  ]
};